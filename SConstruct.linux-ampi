
TOPDIR  = GetLaunchDir()

print TOPDIR + '/include'

env = Environment (
	CXX         = '/home/bordner/charm/charm-cvs/bin/ampicxx',
	CPPFLAGS    = '-Wall -g',
	CPPPATH     = '#/include',
	FORTRANPATH = '#/include',
        FORTRAN     = 'gfortran',
        FORTRANLIBS = 'gfortran',
	LIBPATH     = '#/lib',
	BINPATH     = '#/bin',
         )

Export('env')

SConscript('src/SConscript')
SConscript('test/SConscript')



