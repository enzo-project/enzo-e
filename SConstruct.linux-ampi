
TOPDIR  = GetLaunchDir()

print TOPDIR + '/include'

env = Environment(
	CXX         = '/home/bordner/charm/charm-cvs/bin/charmc -language ampi',
	CPPFLAGS    = '-Wall -g',
	CPPPATH     = '#/include',
	FORTRANPATH = '#/include',
        FORTRAN     = 'gfortran',
        FORTRANLIBS = 'gfortran',
	LIBPATH     = '#/lib',
	BINPATH     = '#/bin',
         )

Export('env')

SConscript('src/SConscript')
SConscript('test/SConscript')



