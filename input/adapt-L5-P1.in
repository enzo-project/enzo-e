# Problem: 2D Implosion problem
# Author:  James Bordner (jobordner@ucsd.edu)

include "input/adapt-L1-P1.in"

Mesh    { 
   root_size   = [16,16];
   max_level   = 5; 
   adapt {balance = false; }
}

Initial {  max_level = 5; }

Output {
      density {
             name = ["adapt-L5-P1-density-%f.png", "time"]; 
      };
      mesh {
             name = ["adapt-L5-P1-mesh-%f.png", "time"]; 
      }
}
