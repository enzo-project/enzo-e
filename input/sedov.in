# Problem: 2D Implosion problem
# Author:  James Bordner (jobordner@ucsd.edu)


# Default: 64^3 domain, 16^3 block sizes, with sedov blast in each block

include "input/sedov-amr.incl"

Mesh    {  max_level = 5; }
Initial {  max_level = 5; }

include "input/sedov-output.incl"

# 3D PROBLEM

# include "input/sedov_array_3.incl"
# Mesh { root_size      = [32,32,32]; }
# Mesh { root_blocks    = [2,2,2]; }
# Enzo { sedov { array = [2,2,2]; } }
# Output { file_groups = ["de","te","mesh"]; }

# 2D PROBLEM

include "input/sedov_array_2.incl"
Mesh { root_size      = [64,64]; }
Mesh { root_blocks    = [2,2]; }
#Mesh { root_size      = [8,8]; }
#Mesh { root_blocks    = [1,1]; }
Enzo { sedov { array = [2,2]; } }
# Output { file_groups = ["de","te","mesh"]; }
# Output { file_groups = ["de","te","mesh","data"]; }
Output { file_groups = ["mesh"]; }
# Output{  mesh { image_mesh_color = "process"; }}

Enzo { sedov { radius_relative = 0.03; } }

Stopping { cycle = 10; }

