`# Problem: 2D Implosion problem
# Author:  James Bordner (jobordner@ucsd.edu)

include "input/adapt-L1-P1.in"

Mesh    {
   root_size   = [64,64];
    max_level   = 3; 
}

Initial {  max_level = 3; }

Output {
      density { 
             name = ["adapt-L3-P1-density-%f.png", "time"]; 
              schedule = ["time","interval", 0.005];
};
      mesh { 
             name = ["adapt-L3-P1-mesh-%f.png", "time"]; 
     }
}
