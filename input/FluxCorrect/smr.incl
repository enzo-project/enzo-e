# Effectively specifies static mesh refinement

 Adapt {
     list = [ "mask" ];
     mask {
         type = "mask";
         value = [10.0, x<0.5 && y < 0.75 && z < 0.75, 0.0];
     }
     max_level = 1;
 }
 