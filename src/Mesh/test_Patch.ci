mainmodule test_Patch {
  readonly CProxy_Main mainProxy;

  mainchare Main {
    entry Main(CkArgMsg *m);
  };
};

module patch {
   chare Patch {
      entry Patch
	(int nx,   int ny,  int nz,
	 int nbx,  int nby, int nbz);
   };
};
