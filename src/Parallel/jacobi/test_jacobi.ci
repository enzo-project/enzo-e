mainmodule test_jacobi {

  readonly CProxy_Main mainProxy;

  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void p_next(double norm);
  };

  array [3D] Patch {
    entry Patch(int block_count, int block_size, CProxy_Main main_proxy);
    entry void p_evolve();
    entry void p_receive(int axis, int face, int n, double buffer[n*n]);
  };

};
