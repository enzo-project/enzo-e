mainmodule test_jacobi {

  readonly CProxy_Main mainProxy;

  mainchare Main {
    entry Main(CkArgMsg *m);
  };

  array [3D] Patch {
     entry Patch();
     entry void p_evolve(int block_count, int block_size);
     entry void p_receive(int dir, int n, double buffer[n*n]);
  };

};
